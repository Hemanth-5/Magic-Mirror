{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "build" } }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/favicon.ico", "dest": "/favicon.ico" },
    { "src": "/manifest.json", "dest": "/manifest.json" },
    { "src": "/logo(.*).png", "dest": "/logo$1.png" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "buildCommand": "npm install javascript-obfuscator --no-save && npm run build && npx javascript-obfuscator ./build/static/js/*.js --output ./build/static/js/ --config ./obfuscator.config.js"
}